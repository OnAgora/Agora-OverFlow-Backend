const categoryResult = require('./category.response');
const collectionsResult = require('./collections.response');
const loginResult = require('./login.response');
const nftsResult = require('./nfts.response');
const result = require('./result');
const userResult = require('./user.response');

module.exports = {
    result,
    loginResult,
    userResult,
    collectionsResult,
    categoryResult,
    nftsResult,
}